id: create-animals-table-from-template
order: 2
templateName: sql-template
templateConfiguration:
    executionSql: |
        CREATE TABLE Animals (
            PersonID int,
            LastName varchar(255),
            FirstName varchar(255),
            Address varchar(255),
            City varchar(255)
        )
    rollbackSql: |
        DROP TABLE IF EXISTS Animals
